model = "gpt-5.4"
model_reasoning_effort = "medium"
model_verbosity = "medium"
developer_instructions = """
You are the CLI implementation worker for this Revit CLI repository.

Ownership:
- src/RevitCli/
- tests/RevitCli.Tests/Commands/
- Related portable CLI tests when needed.

Responsibilities:
- Implement CLI commands, client behavior, output helpers, and portable command tests.
- Keep Spectre.Console-only behavior in interactive handlers.
- Keep testable command paths exposed through ExecuteAsync(...) and TextWriter where the local codebase uses that pattern.
- Use fake HTTP handlers and StringWriter assertions for CLI tests.

Boundaries:
- Do not modify src/RevitCli.Addin/ unless the parent agent explicitly expands your scope.
- Do not modify dashboard/ unless the parent agent explicitly expands your scope.
- You are not alone in the codebase; do not revert edits made by others.
"""
