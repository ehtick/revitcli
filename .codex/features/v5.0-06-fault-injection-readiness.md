---
feature: v5.0/fault-injection-readiness
status: completed-portable
owner: orchestrator
created: 2026-05-22
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~IssueCommandTests|FullyQualifiedName~DeliverablesCommandTests|FullyQualifiedName~RollbackCommandTests|FullyQualifiedName~ReleaseCommandTests|FullyQualifiedName~WorkbenchCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "rg -n \"fault injection|tampered receipt|missing receipt|path permission|stale value\" docs/roadmap-v5-v6.md docs/release-checklist.md docs/v5-demo-and-pilot-playbook.md"
scope-paths:
  - src/RevitCli/Commands/IssueCommand.cs
  - src/RevitCli/Commands/DeliverablesCommand.cs
  - src/RevitCli/Commands/RollbackCommand.cs
  - src/RevitCli/Commands/ReleaseCommand.cs
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - src/RevitCli/Output/DeliveryManifestReader.cs
  - tests/RevitCli.Tests/Commands/IssueCommandTests.cs
  - tests/RevitCli.Tests/Commands/DeliverablesCommandTests.cs
  - tests/RevitCli.Tests/Commands/RollbackCommandTests.cs
  - tests/RevitCli.Tests/Commands/ReleaseCommandTests.cs
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
  - docs/roadmap-v5-v6.md
  - docs/release-checklist.md
  - docs/v5-demo-and-pilot-playbook.md
  - .codex/features/v5.0-06-fault-injection-readiness.md
forbidden-paths:
  - dashboard/**
  - .github/workflows/**
  - .codex/agents/**
  - .codex/config.toml
  - .codex/prompts/**
  - .codex/CONTRACT.md
---

## Checklist

- [x] **impact-scan** (repo-explorer) — Map existing failure handling for missing profiles, missing receipts, unreadable/tampered receipts, path failures, and stale rollback conflicts.
  - verify: `rg -n "missing|tamper|receipt|rollback|permission|fault" src/RevitCli tests/RevitCli.Tests docs`

- [x] **issue-package-faults** (cli-implementer) — Ensure `issue preflight/package` fail clearly for missing profiles/artifacts, hidden writes, bundle path conflicts, and package write failures without partial artifacts.
  - depends-on: impact-scan
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "IssueCommandTests"`

- [x] **delivery-receipt-faults** (cli-implementer) — Ensure deliverables verification/bundling reports missing/unreadable/tampered child receipts and output-path failures with stable codes.
  - depends-on: impact-scan
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "DeliverablesCommandTests"`

- [x] **rollback-faults** (cli-implementer) — Ensure rollback rejects tampered, mismatched, malformed, or stale plan receipts before writes.
  - depends-on: impact-scan
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "RollbackCommandTests"`

- [x] **workbench-fault-index** (test-author) — Add or tighten workbench v2 checks so fault-injection coverage is visible in the contract.
  - depends-on: delivery-receipt-faults
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "WorkbenchCommandTests"`

- [x] **docs-fault-path** (spark-drafter) — Document the portable and Windows/Revit fault-injection matrix without claiming live Revit results from WSL.
  - depends-on: issue-package-faults
  - verify: `rg -n "fault injection|tampered receipt|missing receipt|path permission|stale value" docs/roadmap-v5-v6.md docs/release-checklist.md docs/v5-demo-and-pilot-playbook.md`

- [x] **final-portable-gate** (test-author) — Run focused tests, workbench v2, full portable tests, and `git diff --check`; record exact evidence in Notes.
  - depends-on: docs-fault-path
  - verify: `dotnet test tests/RevitCli.Tests/ && git diff --check`

## Notes

- Keep this as a failure-semantics hardening slice. Do not add new model mutation features.
- Portable tests can cover local files, receipts, JSON schemas, and rollback conflicts. Worksharing locks and true path permissions remain Windows/Revit smoke items unless they can be simulated locally.
- Repo-explorer `019e4f2c-53e2-7240-a022-59d6aaa34ba6` confirmed rollback validation was already strong and identified package/bundle sidecar cleanup plus missing manifest/receipt fault tests as the smallest safe slice.
- Implemented symmetric bundle/receipt cleanup on issue package and delivery bundle write failures, including `receipt-write-failed` / `bundle-receipt-write-failed` codes when failure happens after the ZIP stage.
- Added tests for missing issue profile, issue package write failure cleanup, malformed delivery manifest, malformed child receipt JSON, and delivery bundle receipt-write cleanup.
- Workbench v2 now includes `v5FaultInjectionCoverage`, a temp-dir runtime self-check for missing profile, malformed manifest/receipt, and package write cleanup.
- Focused validation passed: `dotnet test tests/RevitCli.Tests/ --filter "FullyQualifiedName~IssueCommandTests|FullyQualifiedName~DeliverablesCommandTests|FullyQualifiedName~RollbackCommandTests|FullyQualifiedName~ReleaseCommandTests|FullyQualifiedName~WorkbenchCommandTests"` (163 tests).
- Workbench v2 validation passed: `success=true`, `checkCount=52`, `issueCount=0`, including `v5FaultInjectionCoverage`.
- Code-reviewer `019e4f3e-4d98-7672-bc50-fe01776bd89e` found two issues; fixes integrated: missing receipt is now part of `v5FaultInjectionCoverage`, and workbench command-line generation no longer emits an unsafe absolute `--dir` for apostrophe paths.
- Code-reviewer `019e4f42-4441-77f3-8a35-77a74c7da254` confirmed the missing-receipt runtime self-check is exercised and found only the apostrophe path portability issue; final fix uses `workingDirectory` and avoids embedding the unsafe path in command strings.
- Post-review focused validation passed: `dotnet test tests/RevitCli.Tests/ --filter "FullyQualifiedName~WorkbenchCommandTests"` (49 tests).
- Final full portable validation passed: `dotnet test tests/RevitCli.Tests/` (1281 tests).
- `git diff --check` passed.
