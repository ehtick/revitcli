---
feature: v5.5/view-coordination-hygiene
status: complete
owner: orchestrator
created: 2026-05-22
entry-gates:
  - "v5.0 Revit 2026-only RC gate is GO"
  - "v5.1-v5.4 live fixture gaps remain explicitly disclosed"
  - "v5.5 stays audit-first and does not introduce coordinate moves, SaaS, MCP, dashboard-central, or built-in LLM runtime behavior"
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~ViewsCommandTests|FullyQualifiedName~LinksCommandTests|FullyQualifiedName~ModelCommandTests|FullyQualifiedName~WorkbenchCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "dotnet run --project src/RevitCli -- release verify --strict --output json"
scope-paths:
  - docs/smoke/v5.5/
  - docs/roadmap-v5-v6.md
  - src/RevitCli/Commands/ViewsCommand.cs
  - src/RevitCli/Commands/LinksCommand.cs
  - src/RevitCli/Commands/ModelCommand.cs
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - tests/RevitCli.Tests/Commands/ViewsCommandTests.cs
  - tests/RevitCli.Tests/Commands/LinksCommandTests.cs
  - tests/RevitCli.Tests/Commands/ModelCommandTests.cs
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
forbidden-paths:
  - dashboard/**
  - mcp/**
  - src/RevitCli.Addin/**
  - tests/RevitCli.Addin.Tests/**
  - .codex/agents/**
  - .codex/config.toml
---

## Purpose

Harden v5.5 as an audit-first view and coordination hygiene lane. The slice
keeps view, link, workset, and phase risks visible from the terminal while
deferring production write claims until controlled Revit/worksharing pilots
exist.

## Checklist

- [x] **entry-gate-check** (orchestrator) — Proceed by explicit decision after
  v5.4 portable hardening while v5.1-v5.4 live fixture gaps remain unclaimed.
  - verify: `dotnet run --project src/RevitCli -- release verify --strict --output json`
  - evidence: `docs/smoke/v5.5/gap-report.md` records audit-first scope and deferred worksharing/coordinate gates.

- [x] **view-placed-guards** (cli-implementer) — Lock placed-view evidence and
  rollback guard contracts for view template and clone plans.
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "ViewsCommandTests|WorkbenchCommandTests"`
  - evidence: `v55ViewCoordinationHygieneGate` requires frozen view/template ids, clone collision checks, and placed-view rollback guard fields.

- [x] **coordination-no-coordinate-moves** (cli-implementer) — Keep link repair
  limited to path/load changes and keep model map writes behind write-precheck
  evidence.
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "LinksCommandTests|ModelCommandTests|WorkbenchCommandTests"`
  - evidence: `linkRepairNoCoordinateMove`, `modelMapWritableProbe`, and `coordinationReceiptPaths` are bundled into the v5.5 workbench gate.

- [x] **workbench-v55-gate** (orchestrator) — Add a v5.5 workbench gate that
  combines gap disclosure with portable contract evidence.
  - verify: `dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json`
  - evidence: workbench v2 passes with `v55ViewCoordinationHygieneGate`; release strict passes with `v5.5:*` gap disclosure checks.

## Notes

- No coordinate moves in v5.5.
- `links audit` may report coordinate drift; `links repair` may not repair it.
- `model map-fix` remains gated by dry-run plan, write-precheck evidence, and
  receipt/rollback paths; live worksharing lock behavior is deferred.
- Completion note: all checklist items are complete; release/workbench gates
  currently cover the audit-first portable contract while live worksharing and
  coordinate-repair pilots remain explicit gaps.
