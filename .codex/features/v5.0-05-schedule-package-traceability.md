---
feature: v5.0/schedule-package-traceability
status: completed-portable
owner: orchestrator
created: 2026-05-22
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~SchedulesCommandTests|FullyQualifiedName~DeliverablesCommandTests|FullyQualifiedName~IssueCommandTests|FullyQualifiedName~WorkbenchCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "rg -n \"schedule export|manifest|receipt|issue package|bundle hash\" docs/roadmap-v5-v6.md docs/v5-demo-and-pilot-playbook.md README.md"
scope-paths:
  - src/RevitCli/Commands/SchedulesCommand.cs
  - src/RevitCli/Commands/DeliverablesCommand.cs
  - src/RevitCli/Commands/IssueCommand.cs
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - tests/RevitCli.Tests/Commands/SchedulesCommandTests.cs
  - tests/RevitCli.Tests/Commands/DeliverablesCommandTests.cs
  - tests/RevitCli.Tests/Commands/IssueCommandTests.cs
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
  - docs/roadmap-v5-v6.md
  - docs/v5-demo-and-pilot-playbook.md
  - README.md
  - .codex/features/v5.0-05-schedule-package-traceability.md
forbidden-paths:
  - dashboard/**
  - .github/workflows/**
  - .codex/agents/**
  - .codex/config.toml
  - .codex/prompts/**
  - .codex/CONTRACT.md
---

## Checklist

- [x] **impact-scan** (repo-explorer) — Map current `schedules batch-export/compare`, deliverables bundle/verify, and `issue package` traceability behavior plus tests.
  - verify: `rg -n "batch-export|compare|manifest|receipt|issue package" src/RevitCli/Commands tests/RevitCli.Tests/Commands`

- [x] **schedule-export-evidence** (cli-implementer) — Ensure schedule export/compare reports expose enough manifest and hash evidence for issue-day package review without writing hidden files in dry-run.
  - depends-on: impact-scan
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "SchedulesCommandTests"`

- [x] **package-traceability-checks** (cli-implementer) — Harden `issue package` and/or deliverables verification so bundled files can be tied to manifest entries, receipts, hashes, and package receipt evidence.
  - depends-on: impact-scan
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "DeliverablesCommandTests|IssueCommandTests"`

- [x] **workbench-v2-traceability** (test-author) — Add workbench v2 checks for schedule/package traceability gaps that would weaken issue-day closure.
  - depends-on: package-traceability-checks
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "WorkbenchCommandTests"`

- [x] **docs-traceability-path** (spark-drafter) — Update README/playbook/roadmap with the exact schedule export -> compare -> issue package evidence path.
  - depends-on: schedule-export-evidence
  - verify: `rg -n "schedule export|manifest|receipt|issue package|bundle hash" docs/roadmap-v5-v6.md docs/v5-demo-and-pilot-playbook.md README.md`

- [x] **final-portable-gate** (test-author) — Run focused tests, workbench v2, full portable tests, and `git diff --check`; record exact evidence in Notes.
  - depends-on: docs-traceability-path
  - verify: `dotnet test tests/RevitCli.Tests/ && git diff --check`

## Notes

- Keep this as a v5.0 hardening slice; do not add new schedule authoring features.
- Dry-run package planning must remain no-write by default.
- Real Revit schedule export validation remains a Windows/Revit smoke item.
- Repo-explorer `019e4f17-265d-70e3-9bc1-4fc87eb9c282` confirmed the main gaps were missing schedule export/diff digests and package child-file digests.
- Implemented additive `sha256`/byte evidence for `schedule-export-manifest.v1`, before/after path/hash evidence for `schedule-diff-report.v1`, per-file hashes in delivery bundle reports/receipts, and per-file hashes in `issue-package-receipt.v1`.
- Focused portable validation passed: `dotnet test tests/RevitCli.Tests/ --filter "FullyQualifiedName~SchedulesCommandTests|FullyQualifiedName~DeliverablesCommandTests|FullyQualifiedName~IssueCommandTests|FullyQualifiedName~WorkbenchCommandTests"` (101 tests).
- Workbench v2 validation passed: `dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'`.
- Code-reviewer `019e4f21-88eb-7121-b41a-34a0cc3e09d5` found three issues; fixes integrated: no-whitespace shell operators are blocked, parse failures set `noHiddenMutation=false`, and workbench v2 traceability now runs a temp-dir runtime self-check for populated SHA256 evidence and no-write dry-runs.
- Post-review focused validation passed: `dotnet test tests/RevitCli.Tests/ --filter "FullyQualifiedName~IssueCommandTests|FullyQualifiedName~WorkflowCommandTests|FullyQualifiedName~WorkbenchCommandTests|FullyQualifiedName~SchedulesCommandTests|FullyQualifiedName~DeliverablesCommandTests"` (164 tests).
- Full portable validation passed: `dotnet test tests/RevitCli.Tests/` (1275 tests).
- Post-review workbench v2 validation passed: `dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json` returned `success=true`, `checkCount=51`, `issueCount=0`.
- `git diff --check` passed.
