---
feature: v5.0/tick-control-plane-safety
status: completed-portable
owner: orchestrator
created: 2026-05-22
exit-criteria:
  - "bash -n scripts/codex-tick.sh"
  - "rg -n \"pre-existing dirty|worker-created|status: blocked|not marking checkbox\" scripts/codex-tick.sh .codex/prompts/worker.md docs/codex-orchestration.md .codex/CONTRACT.md"
  - "git diff --check"
scope-paths:
  - scripts/codex-tick.sh
  - .codex/prompts/worker.md
  - docs/codex-orchestration.md
  - .codex/CONTRACT.md
  - .codex/features/v5.0-08-tick-control-plane-safety.md
forbidden-paths:
  - src/**
  - shared/**
  - tests/**
  - dashboard/**
  - .github/**
---

## Checklist

- [x] **scope-preservation** (cli-implementer) — Preserve pre-existing dirty
  worktree paths and only revert out-of-scope edits created during the current
  tick.
  - verify: `bash -n scripts/codex-tick.sh`

- [x] **completion-gate** (cli-implementer) — Do not mark the checkbox done
  when the worker set `status: blocked` or produced no worker-created changes.
  - depends-on: scope-preservation
  - verify: `rg -n "status: blocked|not marking checkbox|worker-created" scripts/codex-tick.sh`

- [x] **docs-contract** (spark-drafter) — Update the worker prompt and
  orchestration docs so they describe preservation of pre-existing dirty work.
  - depends-on: completion-gate
  - verify: `rg -n "pre-existing dirty|worker-created|not marking checkbox" .codex/prompts/worker.md docs/codex-orchestration.md .codex/CONTRACT.md`

- [x] **final-shell-gate** (test-author) — Run shell syntax and whitespace
  checks.
  - depends-on: docs-contract
  - verify: `bash -n scripts/codex-tick.sh && git diff --check`

## Notes

- This is a control-plane safety fix prompted by code-reviewer findings. It is
  not a Revit feature and must not touch CLI, add-in, tests, dashboard, SaaS,
  MCP, or LLM behavior.
- Implemented path-baseline scope enforcement with
  `.codex/state/git-paths-before.txt`, `.codex/state/git-paths-after.txt`, and
  `.codex/state/git-paths-created-this-tick.txt`. Pre-existing dirty paths are
  logged and preserved; only worker-created out-of-scope or forbidden paths are
  reverted.
- Checkbox completion now refuses to mark done when the feature has
  `status: blocked` or the tick produced no worker-created changes.
- Validation passed: `bash -n scripts/codex-tick.sh`; docs/contract `rg` check
  for pre-existing dirty, worker-created, blocked, and not-marking behavior;
  `git diff --check`.
