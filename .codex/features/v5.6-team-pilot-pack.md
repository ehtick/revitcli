---
feature: v5.6/team-pilot-pack
status: complete
owner: orchestrator
created: 2026-05-23
entry-gates:
  - "v5.0 Revit 2026-only RC gate is GO"
  - "v5.1-v5.5 live fixture gaps remain explicitly disclosed"
  - "v5.6 stays terminal-first, local-first, deterministic, dry-run first, and does not introduce SaaS, MCP, dashboard-central, or built-in LLM runtime behavior"
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~TeamPolicyValidatorTests|FullyQualifiedName~WorkbenchCommandTests|FullyQualifiedName~ReleaseCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "dotnet run --project src/RevitCli -- release verify --strict --output json"
scope-paths:
  - docs/smoke/v5.6/
  - docs/templates/codex-recipes/team-pilot.md
  - profiles/team-pilot/
  - src/RevitCli/Team/
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - src/RevitCli/Release/ReleaseVerifier.cs
  - tests/RevitCli.Tests/Team/
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
  - tests/RevitCli.Tests/Commands/ReleaseCommandTests.cs
forbidden-paths:
  - dashboard/**
  - mcp/**
  - src/RevitCli.Addin/**
  - tests/RevitCli.Addin.Tests/**
  - .codex/agents/**
  - .codex/config.toml
---

## Purpose

Harden v5.6 as a Team Pilot Pack for BIM managers and IT. This is not an
enterprise SaaS/dashboard slice; it is a local deployability gate around
installer/doctor evidence, team policy files, receipt retention, support
templates, and pilot postmortems.

## Checklist

- [x] **entry-gate-check** (orchestrator) — Proceed after v5.5 portable gate
  hardening while live worksharing/link fixture gaps remain unclaimed.
  - verify: `dotnet run --project src/RevitCli -- release verify --strict --output json`
  - evidence: v5.0-v5.5 workbench/release gates pass; v5.6 scope excludes SaaS, MCP, dashboard-central, and built-in LLM behavior.

- [x] **team-policy-fixture** (cli-implementer) — Add a local policy fixture and
  validator for install years, receipt retention, required commands, support
  templates, and non-cloud boundaries.
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "TeamPolicyValidatorTests"`
  - evidence: `profiles/team-pilot/.revitcli/team-policy.yml` validates and fails when local-first boundaries or retention minimums are broken.

- [x] **workbench-release-v56-gates** (orchestrator) — Add workbench and release
  gates for the Team Pilot Pack.
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "WorkbenchCommandTests|ReleaseCommandTests"`
  - evidence: `v56TeamPilotPackGate` and `v5.6:*` release checks fail on missing docs/policy and pass for the repo root.

- [x] **pilot-docs** (orchestrator) — Add pilot docs and support templates without
  creating a dashboard/SaaS/LLM/MCP dependency.
  - verify: `rg -n "Team Pilot Pack|receipt retention|supportable error reports|No SaaS|MCP|LLM|dashboard-central" docs/smoke/v5.6 docs/templates/codex-recipes/team-pilot.md`
  - evidence: gap report, install postmortem template, support error report template, and recipe are present.

## Notes

- Use `doctor`, `workbench verify`, `release verify`, `standards validate`, and
  `journal verify` as the team pilot command spine.
- Do not add centralized dashboard, cloud sync, MCP adapter, or built-in LLM
  parser as part of v5.6.
