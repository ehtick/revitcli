---
feature: v5.0/addin-protocol-coverage
status: completed-portable
owner: orchestrator
created: 2026-05-22
exit-criteria:
  - "dotnet build src/RevitCli.Addin -p:RevitYear=2026 -p:RevitInstallDir=\"/mnt/d/revit2026/Revit 2026\" --no-restore"
  - "Windows dotnet test tests/RevitCli.Addin.Tests with -p:RevitInstallDir=\"D:\\revit2026\\Revit 2026\""
  - "dotnet test tests/RevitCli.Tests/"
  - "git diff --check"
scope-paths:
  - tests/RevitCli.Addin.Tests/Integration/ProtocolTests.cs
  - .codex/features/v5.0-09-addin-protocol-coverage.md
forbidden-paths:
  - src/**
  - shared/**
  - dashboard/**
  - .github/**
---

## Checklist

- [x] **impact-diagnosis** (orchestrator) — Confirm Windows add-in protocol
  tests fail because the harness connects to `localhost` while the server binds
  `127.0.0.1`.
  - verify: Windows `dotnet test tests/RevitCli.Addin.Tests/`

- [x] **loopback-harness** (test-author) — Use the same IPv4 loopback host in
  protocol tests that the add-in server publishes.
  - depends-on: impact-diagnosis
  - verify: Windows `dotnet test tests/RevitCli.Addin.Tests/`

- [x] **coordination-endpoint-coverage** (test-author) — Add protocol coverage
  for `/api/views`, `/api/links`, `/api/links/repair`, `/api/model/map`, and
  `/api/model/map/fix`.
  - depends-on: loopback-harness
  - verify: Windows `dotnet test tests/RevitCli.Addin.Tests/`

- [x] **final-validation** (test-author) — Run add-in build/tests on Windows
  path plus portable WSL tests and whitespace checks.
  - depends-on: coordination-endpoint-coverage
  - verify: `dotnet test tests/RevitCli.Tests/ && git diff --check`

## Notes

- This is add-in protocol test coverage only. Do not modify Revit API services,
  CLI behavior, dashboard, SaaS, MCP, or LLM behavior.
- Running the add-in test project does not launch Revit and does not mutate a
  live model.
- WSL add-in build passed with
  `dotnet build tests/RevitCli.Addin.Tests/RevitCli.Addin.Tests.csproj -p:RevitYear=2026 -p:RevitInstallDir="/mnt/d/revit2026/Revit 2026"`.
- Windows add-in protocol tests passed from a D:\temp mirror:
  `dotnet.exe test D:\temp\revitcli-addin-test-20260522190903\tests\RevitCli.Addin.Tests\RevitCli.Addin.Tests.csproj -p:RevitYear=2026 -p:RevitInstallDir="D:\revit2026\Revit 2026"`
  (54 tests).
- Portable WSL validation passed: `dotnet test tests/RevitCli.Tests/`
  (1282 tests) and `git diff --check`.
- Follow-up live smoke passed after this protocol slice: Revit 2026 controlled
  issue-closure smoke is documented in
  `docs/smoke/v5.0/revit-2026-issue-closure.md`.
- Remaining live smoke boundary: Revit 2024 and Revit 2025 are still not live
  verified, so v5.0 RC remains NO-GO under strict release verification.
