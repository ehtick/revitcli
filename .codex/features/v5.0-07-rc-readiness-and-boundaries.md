---
feature: v5.0/rc-readiness-and-boundaries
status: completed-portable
owner: orchestrator
created: 2026-05-22
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~ReleaseCommandTests|FullyQualifiedName~WorkbenchCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "dotnet run --project src/RevitCli -- release verify --output json | jq -e '.success == true'"
  - "dotnet run --project src/RevitCli -- release verify --strict --output json; test $? -eq 1"
  - "rg -n \"v5.0 RC|Claimed live Revit years|experimental|v5RealSmokeDisclosure|issuePackageTraceability|v5FaultInjectionCoverage\" docs/v5-rc-readiness.md docs/release-checklist.md docs/roadmap-v5-v6.md README.md"
scope-paths:
  - .codex/features/v5.0-07-rc-readiness-and-boundaries.md
  - docs/v5-rc-readiness.md
  - docs/roadmap-v5-v6.md
  - docs/release-checklist.md
  - README.md
  - .github/workflows/ci.yml
  - src/RevitCli/Commands/ReleaseCommand.cs
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - src/RevitCli/Release/ReleaseVerifier.cs
  - tests/RevitCli.Tests/Commands/ReleaseCommandTests.cs
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
forbidden-paths:
  - src/RevitCli.Addin/**
  - shared/RevitCli.Shared/**
  - dashboard/**
  - .codex/agents/**
  - .codex/config.toml
  - .codex/prompts/**
  - .codex/CONTRACT.md
---

## Checklist

- [x] **impact-scan** (repo-explorer) — Identify the smallest RC/no-go gaps without expanding the command surface.
  - verify: `rg -n "RC|no-go|experimental|v5RealSmokeDisclosure|issuePackageTraceability|v5FaultInjectionCoverage" docs src/RevitCli tests`

- [x] **rc-boundary-doc** (spark-drafter) — Add an explicit v5.0 RC readiness document with stable P0 commands, experimental/deferred boundaries, claimed live Revit years, and evidence gates.
  - depends-on: impact-scan
  - verify: `rg -n "v5.0 RC|Claimed live Revit years|Stable P0|Experimental|not live verified" docs/v5-rc-readiness.md`

- [x] **release-gate** (cli-implementer) — Extend `release verify` so v5 RC gaps are visible and `--strict` blocks RC when live evidence is missing or undisclosed.
  - depends-on: rc-boundary-doc
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "ReleaseCommandTests"`

- [x] **workbench-boundary-check** (cli-implementer) — Add workbench v2 coverage for the RC boundary disclosure doc.
  - depends-on: rc-boundary-doc
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "WorkbenchCommandTests"`

- [x] **ci-doc-wiring** (spark-drafter) — Make README/checklist/roadmap and Ubuntu CI wording point at v2 workbench verification and strict RC no-go behavior.
  - depends-on: release-gate
  - verify: `rg -n "workbench-contract.v2|release verify --strict|v5.0 RC" README.md docs/release-checklist.md docs/roadmap-v5-v6.md .github/workflows/ci.yml`

- [x] **final-portable-gate** (test-author) — Run focused release/workbench tests, workbench v2, release verify default/strict, full portable tests, and `git diff --check`; record evidence.
  - depends-on: workbench-boundary-check
  - verify: `dotnet test tests/RevitCli.Tests/ && git diff --check`

## Notes

- Keep this as a release-boundary slice. It must not add new Revit mutations,
  add-in API endpoints, dashboard workflows, SaaS, MCP, or built-in LLM
  behavior.
- Default `release verify` should remain suitable for Ubuntu CI. `release
  verify --strict` is the RC blocker for the claimed live Revit years.
- Live Revit evidence remains separate from portable WSL checks. Revit 2026 is
  the claimed live RC target; Revit 2024 and Revit 2025 remain disclosed,
  non-claimed gaps.
- Repo-explorer `019e4f49-2528-7850-a33e-307058394b2e` found the concrete gap:
  workbench already checks v5 evidence, but release verification did not make
  RC/no-go status visible or blocking under strict release mode.
- Added `docs/v5-rc-readiness.md` as the stable P0 vs experimental/deferred
  boundary. Current status is GO for a Revit 2026-only RC; Revit 2024 and
  Revit 2025 v5.0 issue-closure smoke evidence has not been recorded.
- `release verify` now reports v5 RC readiness, smoke disclosure, and no-go
  warnings. Default verification remains suitable for Ubuntu CI; `--strict`
  fails while the current no-go warnings are present.
- `workbench verify --contract workbench-contract.v2` now includes
  `v5RcBoundaryDisclosure`.
- Focused validation passed:
  `dotnet test tests/RevitCli.Tests/ --filter "FullyQualifiedName~ReleaseCommandTests|FullyQualifiedName~WorkbenchCommandTests"`
  (55 tests).
- Latest command validation passed: workbench v2 success now reports Revit 2026
  issue-closure evidence; default and strict `release verify` pass for the
  claimed Revit 2026 target while Revit 2024/2025 remain disclosed non-claimed
  gaps.
- Full portable validation passed: `dotnet test tests/RevitCli.Tests/`
  (1282 tests).
- `git diff --check` passed.
- Code-reviewer `019e4f51-0b82-75c2-b423-40dfaf54b249` could not review this
  uncommitted slice directly because its contract targets `HEAD`. Its findings
  were for older `scripts/codex-tick.sh` and add-in endpoint coverage paths, not
  files modified by v5.0-07; those are recorded as follow-up risks rather than
  blockers for this slice.
