---
feature: v5.4/standards-runtime-pack
status: complete
owner: orchestrator
created: 2026-05-22
entry-gates:
  - "v5.0 Revit 2026-only RC gate is GO"
  - "v5.1-v5.3 live fixture gaps remain explicitly disclosed"
  - "v5.4 stays local/offline and does not introduce SaaS, MCP, dashboard-central, or built-in LLM runtime behavior"
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~StandardsCommandTests|FullyQualifiedName~WorkbenchCommandTests|FullyQualifiedName~ReleaseCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "dotnet run --project src/RevitCli -- release verify --strict --output json"
scope-paths:
  - profiles/office-standard/
  - docs/smoke/v5.4/
  - docs/roadmap-v5-v6.md
  - README.md
  - docs/templates/codex-recipes/standards-bootstrap.md
  - src/RevitCli/Standards/
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - src/RevitCli/Release/ReleaseVerifier.cs
  - tests/RevitCli.Tests/Commands/StandardsCommandTests.cs
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
  - tests/RevitCli.Tests/Commands/ReleaseCommandTests.cs
forbidden-paths:
  - dashboard/**
  - mcp/**
  - src/RevitCli.Addin/**
  - tests/RevitCli.Addin.Tests/**
  - .codex/agents/**
  - .codex/config.toml
---

## Purpose

Make office standards executable as a local standards runtime pack. v5.4 should
prove that a BIM manager can preview, install, validate, and review a standards
pack from the terminal without uploading models or relying on an agent runtime.

## Checklist

- [x] **entry-gate-check** (orchestrator) — Proceed by explicit decision after
  v5.1-v5.3 portable hardening while live fixture gaps remain unclaimed.
  - verify: `dotnet run --project src/RevitCli -- release verify --strict --output json`
  - evidence: `docs/smoke/v5.4/gap-report.md` records local/offline scope and deferred pilot gates.

- [x] **office-standard-pack-fixture** (cli-implementer) — Add canonical
  `profiles/office-standard` with profile, workflow, output path, schedule
  template, sheet map, numbering rules, and family-rule references.
  - verify: `revitcli standards validate --manifest .revitcli/standards.yml --dir profiles/office-standard`

- [x] **deterministic-install-validate** (test-author) — Lock dry-run install,
  approved install, and validation output shape for the canonical pack.
  - depends-on: office-standard-pack-fixture
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "StandardsCommandTests"`
  - evidence: canonical `profiles/office-standard` dry-run fingerprint, approved install, copied sheet map/numbering rules, malformed runtime YAML rejection, and validate JSON are covered by `StandardsCommandTests`.

- [x] **workbench-release-gates** (cli-implementer) — Add v5.4 standards runtime
  gate checks to workbench v2 and release verification.
  - depends-on: deterministic-install-validate
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "WorkbenchCommandTests|ReleaseCommandTests"`
  - evidence: `v54StandardsRuntimePackGate` passes in workbench v2 with temp dry-run/apply install smoke; release strict exposes `v5.4:*` local/offline docs, manifest, README, recipe, validation, and install-smoke gates.

- [x] **pilot-gap-disclosure** (orchestrator) — Keep `<10 minute` bootstrap and
  BIM manager pilot evidence explicit and unclaimed.
  - depends-on: workbench-release-gates
  - verify: `rg -n "not benchmarked|not live verified|SaaS|MCP|LLM" docs/smoke/v5.4/gap-report.md`
  - evidence: `docs/smoke/v5.4/gap-report.md` keeps timed bootstrap and office pilot unclaimed and excludes SaaS/MCP/built-in LLM runtime behavior.

## Notes

- Standards packs are local files, not SaaS state.
- Dashboard may view reports later, but CLI/JSON/Markdown outputs remain the
  contract.
- Agents may call these commands, but the pack must stay deterministic without
  embedded LLM decisions.
- Completion note: all checklist items are complete; release/workbench gates
  currently cover the local/offline `profiles/office-standard` pack while timed
  bootstrap and BIM manager pilot evidence remain explicit gaps.
