---
feature: v6.0/local-bimops-ledger-contract
status: complete
owner: orchestrator
created: 2026-05-23
entry-gates:
  - "v5.6 Team Pilot Pack portable gates pass"
  - "v6.0 starts contract-first; no operations-ledger runtime implementation in this slice"
  - "v6.0 stays terminal-first, local-first, deterministic, dry-run first, receipt-backed, rollbackable, and auditable"
exit-criteria:
  - "dotnet test tests/RevitCli.Tests/ --filter \"FullyQualifiedName~WorkbenchCommandTests|FullyQualifiedName~ReleaseCommandTests\""
  - "dotnet run --project src/RevitCli -- workbench verify --contract workbench-contract.v2 --dir . --output json | jq -e '.success == true'"
  - "dotnet run --project src/RevitCli -- release verify --strict --output json"
scope-paths:
  - docs/v6-local-bimops-contract.md
  - docs/smoke/v6.0/
  - docs/roadmap-v5-v6.md
  - README.md
  - src/RevitCli/Commands/WorkbenchCommand.cs
  - src/RevitCli/Release/ReleaseVerifier.cs
  - tests/RevitCli.Tests/Commands/WorkbenchCommandTests.cs
  - tests/RevitCli.Tests/Commands/ReleaseCommandTests.cs
forbidden-paths:
  - dashboard/**
  - mcp/**
  - src/RevitCli.Addin/**
  - tests/RevitCli.Addin.Tests/**
  - .codex/agents/**
  - .codex/config.toml
---

## Purpose

Create the v6.0 Local BIMOps Workbench and operations ledger contract baseline.
This is a docs/contract gate only. It must not introduce SaaS, MCP, built-in
LLM behavior, dashboard-central state, or new Revit mutation runtime.

## Checklist

- [x] **entry-gate-check** (orchestrator) - Start from completed v5.6 gates and
  keep v6.0 contract-first.
  - verify: `dotnet run --project src/RevitCli -- release verify --strict --output json`
  - evidence: v5.6 workbench/release gates pass and v6.0 scope is explicitly non-runtime.

- [x] **contract-doc** (orchestrator) - Add a machine-checkable contract for
  Local BIMOps Workbench and Revit Model Operations Ledger behavior.
  - verify: `rg -n "v6.0 Local BIMOps Workbench Contract|Revit Model Operations Ledger|deterministic receipt|receiptHash|rollbackPointer|rollback preconditions|no SaaS" docs/v6-local-bimops-contract.md`
  - evidence: contract defines command spine, ledger record fields, dry-run/approval rules, rollback semantics, audit invariants, and non-goals.

- [x] **gap-report** (orchestrator) - Add a v6.0 gap report that prevents
  overclaiming runtime readiness.
  - verify: `rg -n "contract baseline|bounded set replay apply|not live verified future work|SaaS|MCP|built-in LLM|dashboard-central" docs/smoke/v6.0/gap-report.md`
  - evidence: report separates portable contract gates and bounded Revit 2026
    live smoke evidence from future broader live Revit/office pilot evidence.

- [x] **workbench-release-gates** (orchestrator) - Add v6.0 workbench and release
  gates for the contract baseline.
  - verify: `dotnet test tests/RevitCli.Tests/ --filter "WorkbenchCommandTests|ReleaseCommandTests"`
  - evidence: gates fail on missing contract/gap docs and pass for the repo root.

## Notes

- Do not add an operations ledger database, cloud sync, dashboard state source,
  MCP adapter, or natural-language parser in this feature.
- The contract should make future implementation harder to overclaim: receipt
  records must be deterministic, rollback preconditions must be explicit, and
  audit artifacts must remain local files first.
- Contract phrase hardening integrated: release tests now fail when
  `deterministic receipt`, `receiptHash`, or `rollbackPointer` disappears from
  `docs/v6-local-bimops-contract.md`.
